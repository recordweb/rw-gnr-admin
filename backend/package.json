{
  "name": "rw-gnr-admin-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Admin backend for the RecordWeb Global Namespace Registry (rw-cc-gnr) via Hyperledger Fabric Gateway",
  "type": "module",
  "main": "src/server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.10.0",
    "@hyperledger/fabric-gateway": "^1.5.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.0",
    "express": "^4.19.0"
  }
}
